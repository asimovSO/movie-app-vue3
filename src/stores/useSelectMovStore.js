import { defineStore } from 'pinia'
import { computed, ref } from "vue";
import { useRoute, useRouter } from 'vue-router';
import useFetchMovies from '../hooks/useFetchMovies';

export const useSelectMovStore = defineStore('selectType', () => {
  
})